---
description: Manual agent for creating commits, pushing changes, and opening Pull Requests. Only invoked when the user explicitly calls. HARD RULES: never a single giant commit, always split by layer, always present a commit plan. A feature branch is the default proposal — the user may choose main.
---
**Carrega o agente `committer`** (`.agents/agents/committer.md`). Cria commits, push e PR — invocado manualmente.

$ARGUMENTS
