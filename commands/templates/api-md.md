# API Context — [PROJECT_NAME]

> **Base URL:** `[https://api.example.com/v1]`
> **Protocol:** [REST / GraphQL / gRPC]
> **Last Updated:** [DATE]

---

## 1. Authentication

- **Method:** [Bearer JWT / API Key / OAuth2 / Session / None]
- **Header:** `Authorization: Bearer <token>`
- **Token endpoint:** `POST /auth/login`
- **Token expiry:** [24h / 1h]
- **Refresh:** `POST /auth/refresh`

---

## 2. Endpoints

### Auth
| Method | Path | Description | Auth |
|--------|------|-------------|------|
| POST | `/auth/login` | Authenticate | No |
| POST | `/auth/refresh` | Refresh token | No |
| POST | `/auth/logout` | Invalidate | Yes |

### [Resource]
| Method | Path | Description | Auth |
|--------|------|-------------|------|
| GET | `/[resources]` | List | Yes |
| GET | `/[resources]/:id` | Get one | Yes |
| POST | `/[resources]` | Create | Yes |
| PUT | `/[resources]/:id` | Update | Yes |
| DELETE | `/[resources]/:id` | Delete | Yes |

---

## 3. Response Conventions

**Success:**
```json
{ "data": { ... }, "meta": { "timestamp": "..." } }
```

**List:**
```json
{ "data": [...], "pagination": { "page": 1, "limit": 20, "total": 100, "totalPages": 5 } }
```

**Error:**
```json
{ "error": { "code": "VALIDATION_ERROR", "message": "...", "details": [...] } }
```

---

## 4. Error Codes

| Status | Code | When |
|--------|------|------|
| 400 | `BAD_REQUEST` | Malformed request |
| 401 | `UNAUTHORIZED` | Invalid/missing token |
| 403 | `FORBIDDEN` | Insufficient permissions |
| 404 | `NOT_FOUND` | Resource missing |
| 409 | `CONFLICT` | Duplicate / state conflict |
| 422 | `VALIDATION_ERROR` | Business rule violation |
| 429 | `RATE_LIMITED` | Too many requests |
| 500 | `INTERNAL_ERROR` | Server failure |

---

## 5. Pagination

- **Strategy:** [Offset / Cursor / N/A]
- **Default limit:** 20 | **Max:** 100

---

## 6. Rate Limiting

- **Limit:** [100 req/min / N/A]
- **Headers:** `X-RateLimit-Limit`, `X-RateLimit-Remaining`, `X-RateLimit-Reset`

---

## 7. Webhooks

| Event | Payload | Trigger |
|-------|---------|---------|
| `[event]` | `{ ... }` | [When] |

---
*Generated by context-generator on [DATE]*
