# Data Model — [PROJECT_NAME]

> **Database:** [PostgreSQL / MySQL / SQLite / MongoDB]
> **ORM:** [Prisma / GORM / SQLAlchemy / Drizzle / N/A]
> **Schema files:** [`prisma/schema.prisma` / N/A]
> **Last Updated:** [DATE]

## 1. Entity Overview
[Brief narrative of entities and relationships]

## 2. Entities

### [EntityName]
**Table:** `[table_name]`

| Field | Type | Constraints | Description |
|-------|------|-------------|-------------|
| `id` | `uuid` | PK, auto | Primary key |
| `created_at` | `timestamp` | NOT NULL, now() | Created |
| `updated_at` | `timestamp` | NOT NULL, auto | Modified |
| `[field]` | `[type]` | [constraints] | [description] |

**Relationships:** belongs_to → [Other] via `other_id` | has_many → [Others]
**Indexes:** `(field1, field2)` — UNIQUE
**Business Rules:** [rules]

## 3. Relationships Summary
| From | Type | To | Via |
|------|------|----|-----|
| [A] | belongs_to | [B] | `b_id` |

## 4. Soft Deletes
- **Strategy:** [`deleted_at` / `is_active` / Hard delete / Mixed]

## 5. Migration Notes
- **Tool:** [Prisma Migrate / Flyway / Alembic / go-migrate / N/A]
- Add columns nullable first. Never drop + add in same migration.

---
*Generated by context-generator on [DATE]*
