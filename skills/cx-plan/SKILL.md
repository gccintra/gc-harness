---
name: cx-plan
description: Create a spec-driven task file inline. Stops for user approval before any code is written. No agent spawn, no cold start.
---

Read `../../commands/plan.md` completely and follow its instructions.
Treat any text supplied with the skill invocation as `$ARGUMENTS`.
