# Development Workflows — [PROJECT_NAME]

> **Branch Strategy:** [GitHub Flow / GitFlow / Trunk-Based]
> **CI/CD:** [GitHub Actions / GitLab CI / N/A]
> **Last Updated:** [DATE]

---

## 1. Local Setup

```bash
[install command]       # install deps
cp .env.example .env    # configure env
docker compose up -d    # start services (if applicable)
[migrate command]       # run migrations
[dev-server command]    # start dev server
```

---

## 2. Branching

- **Naming:** `<type>/<id>-<short-desc>`
- **Never commit to:** `main` / `master`

---

## 3. Commits

Conventional Commits: `<type>(<scope>): <description>`

**Types:** `feat`, `fix`, `chore`, `docs`, `refactor`, `test`, `perf`, `ci`

---

## 4. PR Process

**Required before merge:**
- [ ] All CI checks pass
- [ ] [X] approvals
- [ ] Tests pass (threshold: [80%])
- [ ] No unresolved comments

---

## 5. CI/CD

**On PR:** Lint → Type-check → Tests → Coverage → Security → Build

**On merge to main:** Above + Deploy to [staging / production]

---

## 6. Environments

| Environment | URL | Deploy Trigger |
|-------------|-----|----------------|
| Local | `localhost:[port]` | Manual |
| Staging | [URL or N/A] | Merge to main |
| Production | [URL or N/A] | Manual / Release tag |

---

## 7. Monitoring

- **Errors:** [Sentry / N/A]
- **Logs:** [stdout / Loki / N/A]
- **Metrics:** [Grafana / N/A]

---
*Generated by context-generator on [DATE]*
